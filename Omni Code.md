# Omni Code — 产品需求文档（PRD）

**版本**：v2.0 Final  
**更新日期**：2026-09-06  
**产品 Slogan**：为代码而生的本地智能体


## 一、产品概述

### 1.1 产品定位

Omni Code 是一款**面向编程场景的 AI 智能体桌面应用**。用户通过自然语言描述目标，Agent 自主规划、调用工具、编写并执行代码。**模型层支持 API 或本地部署**，用户自由选择推理后端。所有数据默认存储在本地，账户系统纯离线运行。

**一句话描述**：你说目标，它写代码，模型任选，本地优先。

### 1.2 目标用户

- 独立开发者、AI 应用开发者、DevOps 工程师
- 偏好数据可控、灵活选择模型后端的技术人群

### 1.3 核心原则

| 原则           | 说明                                                         |
| :------------- | :----------------------------------------------------------- |
| **模型自由**   | 支持云端 API（OpenAI/Anthropic/月之暗面等）或本地部署（Ollama/llama.cpp/LM Studio/vLLM），用户随意切换 |
| **本地优先**   | 所有对话、记忆、配置、账户数据默认存储在本地，不上传任何云端（API 调用除外） |
| **透明可审计** | Agent 的每一步思考与操作均对用户可见                         |
| **用户主导**   | 危险操作必须审批，用户始终拥有最终决定权                     |
| **高度可扩展** | 支持自定义工具插件，用户可自由扩展 Agent 能力                |
| **为代码优化** | 所有设计围绕代码编写、执行、调试场景展开                     |


## 二、技术栈

| 层级        | 选型                                      | 说明                                           |
| :---------- | :---------------------------------------- | :--------------------------------------------- |
| 桌面框架    | **Tauri v2**                              | Rust 后端 + Web 前端，二进制小，内存占用低     |
| 数据库      | **SQLite v3.45+**                         | 嵌入式单文件，含 `sqlite-vec` 扩展支持向量检索 |
| 前端框架    | **React 18 + TypeScript**                 | 生态成熟                                       |
| UI 组件     | **Radix UI + Tailwind CSS**               | 无头组件库，完全自定义                         |
| 状态管理    | **Zustand**                               | 轻量级                                         |
| Rust 后端   | **Rust 2024 Edition**                     | 通过 `tauri::command` 暴露 API                 |
| HTTP 客户端 | **reqwest**                               | 异步，调用 API 模型和本地模型                  |
| 任务调度    | **tokio**                                 | 异步运行时                                     |
| 日志        | **tracing**                               | 结构化日志                                     |
| 加密        | **RustCrypto (AES-256-GCM)**              | 加密本地存储的 API 密钥                        |
| 插件系统    | **Wasm 插件运行时** 或 **动态链接库加载** | 支持用户自定义工具扩展                         |


## 三、模型层：API 与本地双模式

### 3.1 模型来源（用户自由选择）

| 类型         | 支持列表                                                     | 说明                                          |
| :----------- | :----------------------------------------------------------- | :-------------------------------------------- |
| **云端 API** | OpenAI / Anthropic / 月之暗面 / DeepSeek / 自定义 OpenAI 兼容端点 | 用户填入 API Key 和端点 URL 即可使用          |
| **本地部署** | Ollama / llama.cpp / LM Studio / vLLM                        | 用户启动本地推理服务后，填入端点 URL 即可接入 |

同一台 Omni Code 实例可同时配置多个模型来源，用户在对话时随时切换。

### 3.2 接入方式

**通用原则**：优先使用推理框架的**原生 HTTP API**，若框架未提供原生接口或原生接口功能不完整，则回退到 OpenAI 兼容接口。

| 框架      | 首选接口                                     | 回退接口                              |
| :-------- | :------------------------------------------- | :------------------------------------ |
| Ollama    | `/api/chat`（原生）                          | `/v1/chat/completions`（OpenAI 兼容） |
| llama.cpp | `/v1/chat/completions`（本身即 OpenAI 兼容） | —                                     |
| LM Studio | `/v1/chat/completions`（本身即 OpenAI 兼容） | —                                     |
| vLLM      | `/v1/chat/completions`（本身即 OpenAI 兼容） | —                                     |
| OpenAI    | `/v1/chat/completions`                       | —                                     |
| Anthropic | `/v1/messages`（原生）                       | —                                     |
| 自定义    | 用户自行填写完整 URL 和请求格式模板          | —                                     |

**适配器架构**：

```
┌──────────────────────────────────────────────────┐
│              LLMProvider trait (Rust)            │
│         chat_completion(request) -> response     │
└────────────────────┬─────────────────────────────┘
                     │
     ┌───────────────┼───────────────┬─────────────┐
     ▼               ▼               ▼             ▼
┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐
│OpenAI适配│  │Anthropic │  │ Ollama   │  │llama.cpp │
│  器      │  │  适配器  │  │ 适配器   │  │  适配器  │
└──────────┘  └──────────┘  └──────────┘  └──────────┘
     │               │               │             │
     └───────────────┴───────────────┴─────────────┘
                     │
                     ▼
              reqwest HTTP Client
```

### 3.3 模型管理

- **注册**：用户填写模型名称、端点 URL、API Key（云端必填，本地可选）、框架类型、上下文长度
- **健康检查**：每 30 秒向端点发送请求，标记在线/离线状态
- **切换策略**：
  - 固定模式：用户指定使用某个模型
  - 智能路由（默认）：复杂任务用高能力模型，简单任务用轻量模型
  - 故障转移：主模型不可用时自动切换备用模型
- **性能监控**：记录 TTFT、TPS（本地模型）、每次调用的 Token 消耗，在模型库页面展示

### 3.4 模型配置项

| 配置项         | 说明                       | 默认值 |
| :------------- | :------------------------- | :----- |
| 端点 URL       | 模型服务的完整地址         | —      |
| API Key        | 云端模型必填，本地模型可选 | 空     |
| 模型标识       | 框架中的 model 名称        | —      |
| 上下文窗口     | 影响 Token 计数和分段策略  | 4096   |
| 最大输出 Token | 限制单次生成长度           | 2048   |
| 温度           | 0.0~2.0                    | 0.7    |
| Top-P          | 0.0~1.0                    | 0.9    |


## 四、本地账户系统（完全离线）

### 4.1 核心设计

所有账户数据**仅存储在本地 SQLite**，不向任何云端发送手机号。手机号仅用于身份标识和数据隔离。

| 功能       | 说明                                                         |
| :--------- | :----------------------------------------------------------- |
| 注册/登录  | 手机号 + 密码（Argon2 哈希存储）                             |
| 手机号验证 | 三种模式：短信网关 / 本地模拟（验证码显示在应用内）/ 管理员绕过 |
| 多用户     | 同设备支持多账户，数据完全隔离                               |
| 会话保持   | 登录状态持久化，支持"记住我 30 天"                           |

### 4.2 验证码规则

- 6 位数字，有效期 5 分钟
- 同一号码 60 秒内只能请求一次
- 验证码存储在 SQLite，5 分钟后自动清理

### 4.3 数据隔离

所有业务表（对话、任务、记忆等）均包含 `user_id` 外键，不同用户数据完全隔离，模型配置也按用户分别存储。


## 五、核心功能模块

### 5.1 对话与任务接收

**输入**：
- 自然语言文本（最多 20000 字符）
- 附件（最多 5 个）：`.txt, .pdf, .docx, .xlsx, .csv, .json, .yaml, .png, .jpg, .py, .js, .ts, .rs, .go`，单文件 ≤ 20MB

**处理流程**：
1. 输入净化（去除不可见字符）
2. 语言检测（zh/en）
3. 意图分类（代码生成 / 代码修改 / 调试修复 / 项目初始化 / 代码审查 / 文档生成 / 其他）
4. 实体抽取（文件路径、函数名、变量名、依赖名等）
5. 冲突检测：检查并发任务数是否超限
6. 生成 Task ID：`TSK-{YYYYMMDD}-{HHMMSS}-{UUID前4位}`
7. 写入任务队列，状态标记为 `PENDING`

### 5.2 规划引擎

**输入**：已解析的任务对象（意图、实体、历史摘要）  
**输出**：DAG 执行计划，或需要补充信息的问题列表

**生成逻辑**：
1. 检索当前用户可用工具（内置工具 + 已安装插件）
2. 构建包含角色设定、工具列表、约束条件的提示词
3. 进行 3 次不同温度下的自洽性检查，差异过大则降级为人工确认
4. 循环依赖检测（Kahn 算法）
5. 生成 Plan ID，状态为 `DRAFT`，等待用户确认或自动执行

### 5.3 执行器

**输入**：计划中单个 step 的完整定义  
**输出**：执行结果（成功/失败）+ 结构化返回值

**内置工具分类**：

| 类别            | 工具                                         | 超时 | 风险等级   |
| :-------------- | :------------------------------------------- | :--- | :--------- |
| 文件读取        | read_file                                    | 30s  | 低         |
| 文件写入        | write_file（含创建/修改/覆盖）               | 60s  | 中         |
| 文件删除        | delete_file / delete_directory               | 60s  | 高         |
| 文件移动/重命名 | move_file                                    | 30s  | 中         |
| 目录创建        | create_directory                             | 10s  | 低         |
| 代码执行        | execute_python / execute_shell               | 60s  | 高         |
| Git 操作        | git_status / git_log / git_diff / git_commit | 30s  | 视操作而定 |
| 依赖管理        | npm_install / pip_install                    | 120s | 中         |
| 网络请求        | http_request（GET）                          | 45s  | 低         |
| 网络请求        | http_request（POST/PUT/DELETE）              | 60s  | 高         |
| 搜索            | web_search                                   | 30s  | 低         |

**重试策略**：
- 第 1 次失败 → 立即重试
- 第 2 次失败 → 等待 4 秒后重试
- 第 3 次失败 → 等待 8 秒后重试
- 第 4 次失败 → 标记 `FAILED_PERMANENT`，触发 fallback 或请求人工介入

**安全沙箱（本地）**：
- 工作区限制：所有文件操作限定在用户指定的工作区目录内，路径穿越检测
- Python 执行：限制 CPU 60s、内存 512MB，默认禁用网络（可配置开启）
- AST 静态分析拦截危险调用（`os.system`, `rm -rf` 等）

### 5.4 执行模式（三种）

| 模式                 | 图标 | 行为                                                         |
| :------------------- | :--- | :----------------------------------------------------------- |
| **手动审批**         | 🛡️    | 所有**中风险及以上**操作均弹出审批卡片，等待用户批准         |
| **自动审批（推荐）** | 🤖    | Agent 自行评估风险，低风险直接执行，中高风险时结合上下文判断：置信度 ≥ 90% 自动执行，< 90% 询问用户 |
| **完全访问（危险）** | ⚡    | 所有操作不经任何审批直接执行，UI 显示红色警告横幅，每次启动弹窗提醒 |

### 5.5 审批触发规则（仅与危险程度相关）

**审批触发完全基于操作的风险等级，与步骤数量、任务长度无关。**

| 风险等级                                                     | 手动审批模式 | 自动审批模式                    | 完全访问模式 |
| :----------------------------------------------------------- | :----------- | :------------------------------ | :----------- |
| **低**（文件读取、搜索、GET 请求、目录创建）                 | 自动执行     | 自动执行                        | 自动执行     |
| **中**（文件写入/修改、依赖安装）                            | 触发审批     | 置信度 ≥ 90% 自动执行，否则审批 | 自动执行     |
| **高**（文件删除、代码执行、Shell 命令、POST/PUT/DELETE、Git 推送） | 触发审批     | 触发审批                        | 自动执行     |

**中风险自动审批的判定条件（细化）**：

| 具体操作                   | 自动执行条件                                       | 触发审批条件                      |
| :------------------------- | :------------------------------------------------- | :-------------------------------- |
| write_file（修改已有文件） | 修改行数 ≤ 10 行，且文件大小 ≤ 50KB                | 修改行数 > 10 行，或文件 > 50KB   |
| write_file（创建新文件）   | 文件大小预估 ≤ 100KB                               | 文件大小预估 > 100KB              |
| delete_file                | 始终触发审批                                       | —                                 |
| delete_file（批量 > 3 个） | 始终触发审批，且额外高亮警告                       | —                                 |
| npm_install / pip_install  | 安装包数量 ≤ 3 个，且均为知名包                    | 安装包 > 3 个，或包含非知名包     |
| git_commit                 | 变更文件数 ≤ 5 个，且 commit message 由 Agent 生成 | 变更 > 5 个，或用户未确认 message |

### 5.6 人机交互与审批

**审批 UI**：
- 审批卡片**内联嵌入**对话流，非弹窗
- 显示：操作描述 + 参数详情 + 风险等级标签 + [批准] [修改参数] [拒绝] 按钮
- 用户 2 小时无响应 → 邮件提醒（若已配置 SMTP）；24 小时无响应 → 任务超时取消

### 5.7 记忆系统

**三层架构**：

| 层级       | 存储                  | 说明                                                    |
| :--------- | :-------------------- | :------------------------------------------------------ |
| 短期记忆   | SQLite（会话表）      | 当前会话的完整对话和推理链                              |
| 长期记忆   | SQLite + sqlite-vec   | 跨会话的关键事件、偏好、成功/失败模式，向量检索 Top-K=5 |
| 程序性记忆 | YAML 文件（用户目录） | 用户保存的工作流模版，支持导入/导出                     |

**记忆写入时机**：
- 任务成功 → 写入成功经验（分数 +10）
- 任务失败 → 写入失败教训（分数 +5）
- 用户纠正 Agent → 写入高优先级记忆


## 六、插件系统（高度自定义）

### 6.1 设计目标

用户可自由扩展 Agent 的工具能力，无需修改 Omni Code 核心代码。插件与主应用**隔离运行**，不威胁系统安全。

### 6.2 插件类型

| 类型           | 说明                                         | 适用场景                                            |
| :------------- | :------------------------------------------- | :-------------------------------------------------- |
| **工具插件**   | 新增一个或多个工具函数，Agent 可在规划时调用 | 接入公司内部 API、自定义代码检查规则等              |
| **模型插件**   | 新增一个完整的模型适配器                     | 对接非标准 API 格式的私有模型服务                   |
| **提示词插件** | 注入自定义系统提示词或领域知识               | 为特定框架（如 Django/Spring Boot）定制代码生成风格 |

### 6.3 插件格式

插件以 **Wasm 模块** 或 **动态链接库（.dylib/.so/.dll）** 形式分发，也可通过 **HTTP 远程工具**（用户填写 OpenAPI Schema 端点）动态注册。

**HTTP 远程工具（最轻量，推荐优先支持）**：

用户填写：
- 工具名称 + 描述
- 端点 URL + HTTP 方法
- 请求参数 Schema（JSON Schema 格式）
- 认证方式（API Key Header / Bearer Token）

Agent 在规划时将该工具纳入可用工具列表，调用时发送 HTTP 请求。

**Wasm 插件（安全隔离，推荐高级用户）**：

- 使用 Rust/C/C++ 编写，编译为 Wasm
- 通过 Tauri 的 Wasm 运行时加载
- 受限的系统访问权限（仅限显式授权的 API）
- 主应用提供 SDK，包含 `log`、`http`、`file_read（受限）` 等辅助函数

### 6.4 插件市场（可选）

应用内提供**插件列表浏览**功能（从 GitHub 仓库或用户指定源拉取元数据），用户一键下载安装。插件安装后显示在"能力扩展"页面，可启用/禁用/卸载。

### 6.5 工具优先级

当多个工具功能重叠时（例如内置 `web_search` 和插件提供的 `web_search`），用户可在设置中调整工具优先级顺序，或禁用特定工具。


## 七、用户界面

### 7.1 设计风格

- **参照**：Codex / Trae 风格，清爽对话式界面
- **圆角**：12-16px，现代柔和
- **间距**：宽松（8px 基础单位）
- **暗色模式**：跟随系统偏好，设置中可手动覆盖

### 7.2 布局结构

```
┌─────────────────────────────────────────────────────────┐
│ 标题栏 (应用名 / 当前模型状态 / 窗口控制)              │
├──────────┬──────────────────────────────────────────────┤
│ 侧边导航  │  主内容区                                   │
│          │                                             │
│ 💬 对话   │  对话页面 / 历史记录 / 模型库 / 设置        │
│ 📜 历史   │                                             │
│ 🧠 模型库 │                                             │
│ 🔌 扩展   │  （插件管理入口）                           │
│ ⚙️ 设置   │                                             │
│          │                                             │
│ ──────── │                                             │
│ 👤 用户名 │                                             │
├──────────┴──────────────────────────────────────────────┤
│ 状态栏 (任务数 / 模型状态 / 工作区路径)                 │
└─────────────────────────────────────────────────────────┘
```

### 7.3 对话页面（主界面）

**组成**：
1. 左侧历史列表（240px，可拖拽调整）：对话标题 + 时间 + 状态圆点，支持搜索和右键菜单
2. 右侧消息流（自动滚动）：
   - 用户消息：右对齐，蓝色渐变气泡
   - Agent 消息：左对齐，含推理过程（折叠面板）和工具调用卡片
3. 底部输入区：多行文本 + 附件拖拽上传 + 执行模式下拉 + 模型选择下拉 + 发送按钮
4. 任务控制栏（输入区上方）：进度条 + 暂停/继续/取消按钮

**推理过程折叠**：
- 正在生成时：实时流式显示，**保持展开**
- 推理结束：**自动折叠**为一行摘要
- 用户可点击手动展开/折叠

### 7.4 模型库页面

模型卡片展示：
- 名称 + 来源类型（云端 API / 本地部署）+ 在线状态 + 是否为默认
- 端点 URL + 上下文窗口 + 温度等参数
- 性能指标（TTFT / TPS / Token 消耗）
- 操作按钮：[设为默认] [测试连接] [编辑] [移除]

**添加模型向导**：
- 选择来源类型：云端 API / 本地部署
- 云端 API：下拉选择服务商（OpenAI/Anthropic/月之暗面/DeepSeek/自定义），填入 API Key
- 本地部署：下拉选择框架（Ollama/llama.cpp/LM Studio/vLLM），自动检测默认端口
- 测试连接 → 完成添加

### 7.5 扩展（插件管理）页面

- 已安装插件列表：名称 / 版本 / 类型 / 状态（启用/禁用）
- 操作按钮：[启用/禁用] [卸载] [配置]
- "浏览插件市场"按钮（若已配置源）
- "添加自定义工具"按钮（HTTP 远程工具注册向导）


## 八、全局快捷键

| 快捷键         | 功能                         |
| :------------- | :--------------------------- |
| `Ctrl+N`       | 新建对话                     |
| `Ctrl+1`       | 切换手动审批模式             |
| `Ctrl+2`       | 切换自动审批模式             |
| `Ctrl+3`       | 切换完全访问模式（二次确认） |
| `Ctrl+Enter`   | 发送消息                     |
| `Ctrl+,`       | 打开设置                     |
| `Ctrl+L`       | 锁定屏幕                     |
| `Ctrl+Shift+E` | 导出当前对话                 |
| `Esc`          | 取消当前操作                 |


## 九、数据模型

### 核心表结构

**users**：id / phone / password_hash / phone_verified / display_name / created_at / last_login

**sessions**：id / user_id / token / created_at / expires_at

**conversations**：id / user_id / title / created_at / updated_at

**messages**：id / conversation_id / role（user/agent/system）/ content / reasoning（推理过程）/ created_at

**tasks**：id / conversation_id / user_id / plan_json / status / created_at / updated_at

**steps**：id / task_id / tool_name / params_json / result_json / status / retry_count / start_time / end_time

**memories**：id / user_id / content / embedding（BLOB）/ importance / created_at / last_accessed

**models**：id / user_id / name / source_type（api/local）/ provider / endpoint / api_key_encrypted / model_identifier / context_length / temperature / is_default / status

**plugins**：id / user_id / name / type（tool/model/prompt）/ version / enabled / config_json / installed_at

**custom_tools**：id / user_id / name / description / endpoint / method / schema_json / auth_config / enabled


## 十、非功能需求

| 维度     | 指标                                                |
| :------- | :-------------------------------------------------- |
| 性能     | 首次规划 P95 < 8s；单步骤执行 P99 < 120s            |
| 可用性   | 99.5%（不含 LLM 服务故障）                          |
| 安全     | TLS 1.3 + AES-256-GCM；敏感信息脱敏打印             |
| 最小窗口 | 900 × 650 px                                        |
| 支持平台 | Windows 10+ / macOS 11+ / Linux（AppImage/Deb）     |
| 插件隔离 | Wasm 运行时或独立进程沙箱，防止插件影响主应用稳定性 |


## 十一、安装与首次启动

1. 下载对应平台安装包
2. 首次启动 → 账户注册/登录向导（手机号验证）
3. 选择工作区文件夹（`~/OmniCodeWorkspace/`）
4. 添加模型：
   - 云端 API：选择服务商 → 填入 API Key
   - 本地部署：自动扫描本地推理框架 → 选择已运行的服务
   - 可跳过，后续在设置中添加
5. 进入主界面


## 十二、附录：关键交互示例（编程场景）

### 示例一：重构代码

**用户**：帮我把 `src/utils/string_helper.rs` 里的所有 `unwrap()` 改成 `?` 操作符，并处理对应的错误类型。

**Agent**（规划后显示）：
> 我计划分 3 步执行：
> 1. 读取 `src/utils/string_helper.rs`
> 2. 分析每个 `unwrap()` 调用点的返回类型，推断对应的错误类型
> 3. 逐个替换为 `?`，并调整函数签名（返回 `Result<T, E>`）
>
> 当前模式：手动审批 | 是否开始？

**用户**：开始。

**Agent**（推理中展开）：
> [思考] 正在扫描文件中的 `unwrap()` 调用，共发现 4 处。
> 第 7 行：`parse()` 返回 `Result<u64, ParseIntError>` → 应替换为 `?`
> 第 23 行：`fs::read_to_string()` 返回 `Result<String, io::Error>` → 应替换为 `?`

**推理结束后自动折叠**，工具调用卡片显示：
- ✅ read_file 完成 (2.3ms)
- 等待审批：write_file → 显示 diff 变更，用户确认后写入

### 示例二：生成单元测试

**用户**：为 `api/handlers/user.rs` 里的 `create_user` 函数生成完整的单元测试，覆盖边界情况。

**Agent**：
> 我将执行：
> 1. 读取 `api/handlers/user.rs`，理解 `create_user` 的实现逻辑
> 2. 分析函数参数、返回值及依赖
> 3. 生成测试用例，覆盖：正常输入、空字段、超长字段、错误模拟
>
> 是否继续？

**用户**：继续。

**Agent**（直接输出代码块）：
```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_create_user_success() { ... }
    #[test]
    fn test_create_user_empty_username() { ... }
}
```

### 示例三：调试与修复

**用户**：CI 报错了，`npm run build` 在 `src/components/Chart.tsx` 第 42 行有类型错误，帮我看一下。

**Agent**：
> 我将执行：
> 1. 读取 `src/components/Chart.tsx` 及 `tsconfig.json`
> 2. 运行 `npx tsc --noEmit` 获取完整错误信息
> 3. 分析类型不匹配原因，给出修复方案
>
> 当前模式：自动审批 | 确认执行？

**用户**：确认。

**Agent**（推理后）：
> [已分析] 错误原因：`data` 属性类型为 `Record<string, number>`，但 `BarChart` 的 `series` 期望 `Array<{ name: string; value: number }>`。
>
> 修复方案：将第 42 行改为 `data={Object.entries(rawData).map(([name, value]) => ({ name, value }))}`
>
> 需要我直接修改文件吗？

### 示例四：项目初始化

**用户**：用 Vite + TypeScript + Tailwind 初始化一个 React 项目，目录名叫 `dashboard`。

**Agent**：
> 我将执行：
> 1. `npm create vite@latest dashboard -- --template react-ts`
> 2. 安装依赖
> 3. 安装并配置 Tailwind CSS
> 4. 清理默认模板，保留最小可用结构
>
> 预计耗时 30 秒 | 确认？

**用户**：确认。

**Agent**（实时流式输出）：
```
[执行] npm create vite@latest dashboard -- --template react-ts
✓ 项目创建成功
[执行] cd dashboard && npm install
✓ 依赖安装完成 (12.4s)
[执行] npm install -D tailwindcss postcss autoprefixer && npx tailwindcss init -p
✓ Tailwind 配置完成
[执行] 清理 src/ 默认文件...
✓ 保留 src/main.tsx + src/App.tsx（已简化）
```

### 示例五：依赖安全审计

**用户**：检查 `package.json` 里的依赖有没有已知漏洞。

**Agent**：
> 我将执行：
> 1. 读取 `package.json` 和 `package-lock.json`
> 2. 运行 `npm audit --json` 获取漏洞报告
> 3. 对有漏洞的依赖，查询最新安全版本
>
> 此操作不会修改文件，可放心执行。

**用户**：执行。

**Agent**（生成报告）：
```
📊 审计结果：
- 高危：1 个（lodash: 4.17.20 → 4.17.21）
- 中危：2 个（express: 4.18.2 → 4.19.2）

升级命令：npm install lodash@4.17.21 express@4.19.2
是否执行？
```

### 示例六：自定义插件使用

**用户**：查一下我们内部 API 的用户列表。

**Agent**（调用插件工具）：
> [思考] 检测到已安装"内部 API 插件"，正在调用 `get_internal_users` 工具...
>
> 工具调用：`GET https://internal.company.com/api/users`
> 返回结果：200 OK (3 条记录)
>
> 当前系统共有 3 位用户：张三（开发者）、李四（测试）、王五（运维）。
